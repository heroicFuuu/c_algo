#include <stdio.h>

int main() {
    if (printf("geeks"))
        switch (printf("for"))
            // skip, 除非加上 default
            // default:
            while (printf("geeks"))
                return 0;
}
